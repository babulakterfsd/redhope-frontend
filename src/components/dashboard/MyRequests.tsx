const MyRequests = ({ loggedInUser }: any) => {
  console.log('loggedInUser in my requests page', loggedInUser);
  return (
    <div>
      <p>this is my requests page</p>
    </div>
  );
};

export default MyRequests;
